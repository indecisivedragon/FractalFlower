<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.project.lili.fractalflower.TestAnimActivity"

    android:id="@+id/action_test_anim"

    >

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/replay_animation"
        android:onClick="replayAnimation"
        android:id="@+id/reset_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Fade"
        android:onClick="fadeColors"
        android:id="@+id/fade_button"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/reset_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rotate"
        android:id="@+id/rotate_button"
        android:onClick="rotate"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/fade_button" />

</RelativeLayout>
